当检测到读写 `/tmp` 目录下的文件或子目录时：

如果目标路径为 `/tmp/<子目录>/` 且子目录名不含随机数（如 `/tmp/dir1/`、`/tmp/logs/` 等），则存在提权风险——攻击者可预先创建该路径并建立符号链接，劫持程序的文件读写操作。

- 子目录名含随机数（如 `/tmp/upload_1a2b3c/`）→ 风险较低，可结合上下文判断
- 子目录名不含随机数（如 `/tmp/dir1/`）→ 存在提权风险，需标记为疑似漏洞
