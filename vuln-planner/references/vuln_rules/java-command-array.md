当检测到命令以数组形式执行（如 `new String[]{"bash", "-c", userInput}`、`ProcessBuilder("bash", "script.sh")` 等）：

！！首先要纠正数组就一定没有命令注入的问题，必须严格按照我的规则来：

涉及脚本执行时：
如果数组内容指向一个脚本文件（如 `script.sh`、自定义脚本路径）：

- 找到脚本 → 加载并分析该脚本内容，如果外部参数能导致任意命令执行，就可以认为是存在命令注入的问题
- 未找到脚本 → 显式说明"未找到对应脚本，无法确认是否存在命令注入，需人工审查"——不能下结论说"不存在命令注入"
