# 本地提权分析

## 概述

始终排查低权限用户能否构造脚本、配置文件或输入，被高权限进程执行或操作，从而提升权限。

## 常见场景

### 命令名用户可控

bash 执行的命令或脚本名用户可控时存在风险：低权限用户可通过控制命令名，让高权限进程执行 \$PATH 中已有的其他命令，达到非预期的效果。攻击者不一定需要控制 \$PATH 目录中的文件，利用 \$PATH 搜索顺序即可触发非预期执行。
