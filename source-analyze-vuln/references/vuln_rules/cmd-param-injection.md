执行命令参数可控时，需深入分析是否存在以下风险：

- **高危参数注入**：如 `-o /path/output.txt` 可覆盖任意文件、`--output=` 指定任意路径等
- **空格攻击**：参数中注入空格导致命令行为改变，如 `rm` 删除命令通过空格传入额外路径删除非目标文件
- **解析压缩包文件名**：解压时文件名的空格或 `-` 开头部分可能被解析为命令参数

确认是否对参数值做了白名单校验或转义处理。
